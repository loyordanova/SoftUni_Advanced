rows, cols = [int(x) for x in input().split(', ')]

matrix = []

for row in range(rows):
    lines = [int(x) for x in input().split(', ')]
    matrix.append(lines)

total_sum = 0
for x in matrix:
    for y in x:
        total_sum += int(y)
print(total_sum)
print(matrix)
